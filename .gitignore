# Build output
bin/
obj/
publish/
artifacts/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# macOS
.DS_Store

# Runtime data created beside the app (portable storage)
/Data/
/Covers/
/Cache/
/Logs/
/Settings/

# Local source material and game files (selected redistributable assets are copied into src/)
/assets to use/
/roms for testing/

# Bundled/downloaded binaries — CI fetches these at build time (see .github/workflows/build.yml)
rclone.exe
/rclone
*_libretro.so
/arcade/
/screenshots/
