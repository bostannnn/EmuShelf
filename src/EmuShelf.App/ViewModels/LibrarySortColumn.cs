namespace EmuShelf.App.ViewModels;

/// <summary>The list-view columns the library can be sorted by.</summary>
public enum LibrarySortColumn
{
    Title,
    Console,
    Format,
    Achievements,
    Textures,
    Status,
    LastPlayed,
    DateAdded,
}
