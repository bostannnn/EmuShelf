using Avalonia.Controls;

namespace EmuShelf.App.Views;

/// <summary>The setup wizard's step rail (see the markup). Pure view; bound to a <c>SetupWizardRailModel</c>.</summary>
public partial class SetupWizardRailView : UserControl
{
    public SetupWizardRailView()
    {
        InitializeComponent();
    }
}
